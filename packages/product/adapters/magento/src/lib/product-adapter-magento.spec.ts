import { productAdapterMagento } from './product-adapter-magento';

describe('productAdapterMagento', () => {
  it('should work', () => {
    expect(productAdapterMagento()).toEqual('product-adapter-magento');
  });
});
